# Project Guidelines

- Whenever a user may reasonably need or strongly desire to change a setting, any new feature or UI/UX implementation must expose that behavior as a configurable option in the Settings pane.
- Automatically validate every new feature on mobile, tablet, and desktop—using a browser when possible—for misaligned or unreachable components, unscrollable multi-page views, missing platform-specific features, and similar responsive or cross-platform regressions; when reasonably feasible, fix issues found. For native features, also make a quick Android emulator pass when a running emulator or an available SDK AVD exists on the host.
- Any view whose content can exceed its available viewport or pane must remain fully reachable through deliberate scrolling on smaller displays whenever reflowing, wrapping, or stacking the content would not preserve a reliable UI/UX flow and layout; never leave required content clipped, inaccessible, or dependent on hidden overflow.
- Perform this validation using subagents in the following fallback order: 5.6 Luna medium; if unavailable, 5.6 Terra medium; if GPT-based subagents are unavailable, try Opus 4.8 medium.
- After a smaller-model subagent pass, the main (parent) model must independently review both the changes and the subagent's findings before completion or commit, using at least low or medium reasoning effort.
- Remote-backed editable controls must keep draft and last-confirmed values separate; show queued, updating, success, error, and uncertain states; restore the last-confirmed value on an authoritative rejection; reconcile uncertain outcomes with the remote system; and never allow stale responses to overwrite newer state.
- Any action that can reasonably be performed faster, more precisely, or repeatedly with a keyboard must be exposed as a discoverable command in the global Command Palette alongside mouse and touch affordances. Register new keyboard-suitable interactions as commands by default; dedicated shortcuts are optional, while confirmations and remote-state safety requirements remain mandatory.
- Enforce WCAG 2.2 AA contrast at runtime for every bundled, imported, previewed, and installed theme: normal text—including field values, labels, placeholders, chips, and button copy—must reach at least 4.5:1 against its actual rendered background; large text may use 3:1; focus indicators, control boundaries, icons, and other required non-text UI must reach at least 3:1. Automatically repair unsafe theme tokens before rendering, retain readable disabled-control copy even though WCAG exempts inactive controls, never rely on reduced opacity when it makes controls unreadable, and regression-test hostile low-contrast themes across light and dark modes.
